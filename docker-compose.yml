services:
  mongodb:
    image: mongo:7
    container_name: interior-mongo
    ports:
      - "27017:27017"
    volumes:
      - interior_mongo_data:/data/db
    restart: unless-stopped

volumes:
  interior_mongo_data:
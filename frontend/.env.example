# Local: point at Express. Vite also proxies /api → :5000 when unset in some setups.
VITE_API_URL=http://localhost:5000/api/v1

# Production on Vercel (same origin): leave empty or use relative /api/v1
# VITE_API_URL=/api/v1

VITE_WHATSAPP_NUMBER=971550000000
VITE_SITE_NAME=Interior Platform

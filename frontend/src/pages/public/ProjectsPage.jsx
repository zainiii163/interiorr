import StubPage from '../../components/ui/StubPage';
export default function ProjectsPage() {
  return <StubPage title="Projects" description="Filterable portfolio — fetches GET /projects?category=." />;
}
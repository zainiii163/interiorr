import StubPage from '../../components/ui/StubPage';

export default function HomePage() {
  return (
    <StubPage
      title="Home"
      description="Hero, trust pillars, stats, services preview, featured projects, reviews, and CTAs."
    />
  );
}
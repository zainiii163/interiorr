import StubPage from '../../components/ui/StubPage';
export default function ServicesPage() {
  return <StubPage title="Services" description="Full services grid with categories — fetches GET /services." />;
}
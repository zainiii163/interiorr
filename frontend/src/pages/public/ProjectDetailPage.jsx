import StubPage from '../../components/ui/StubPage';
export default function ProjectDetailPage() {
  return <StubPage title="Project Detail" description="Gallery and scope — fetches GET /projects/:slug." />;
}